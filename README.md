# Fits Me Right

- `01-Site-Code` — the React/Vite frontend, deployed to Cloudflare Workers
- `02-Public-Assets` — the source-of-truth image library, synced into the frontend at build time
- `03-Server-Code` — the Express + Prisma API, deployed to Heroku, serving the product catalog from Postgres (see its README for setup and deploy steps)
